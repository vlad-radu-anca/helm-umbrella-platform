{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: mongo-express-service
spec:
  type: NodePort
  selector:
    app: mongo-express
  ports:
    - protocol: TCP
      name: express
      port: 8081
      nodePort: 30444
---
{{- end }}