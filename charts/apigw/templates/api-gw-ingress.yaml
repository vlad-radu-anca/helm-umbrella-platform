
{{- if .Values.apigw.ingress.enabled -}}
{{- $fullName := include "apigw.fullname" . -}}
{{- $svcPort := .Values.apigw.service.port -}}
{{- $endpoint := .Values.endpoint -}}
{{- if and .Values.apigw.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.apigw.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.apigw.ingress.annotations "kubernetes.io/ingress.class" .Values.apigw.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "apigw.labels" . | nindent 4 }}
  {{- with .Values.apigw.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.apigw.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.apigw.ingress.className }}
  {{- end }}
  {{- if .Values.apigw.ingress.tls }}
  tls:
    {{- range .Values.apigw.ingress.tls }}
    - hosts:
        - apigw.{{ $endpoint }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.apigw.ingress.hosts }}
    - host: apigw.{{ $endpoint }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: apigw
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: apigw
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
