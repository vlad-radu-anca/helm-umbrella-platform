{{- if .Values.sftp.init.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "sftp-server.fullname" . }}-init
data:
  init.sh: |
{{ .Values.sftp.init.script | indent 4 }}
{{- end }}
