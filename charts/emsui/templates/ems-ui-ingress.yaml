
{{- if .Values.emsui.ingress.enabled -}}
{{- $fullName := include "emsui.fullname" . -}}
{{- $svcPort := .Values.emsui.service.port -}}
{{- $endpoint := .Values.endpoint -}}
{{- $subdomain := .Values.emsui_subdomain -}}
{{- if and .Values.emsui.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.emsui.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.emsui.ingress.annotations "kubernetes.io/ingress.class" .Values.emsui.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "emsui.labels" . | nindent 4 }}
  {{- with .Values.emsui.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.emsui.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.emsui.ingress.className }}
  {{- end }}
  {{- if .Values.emsui.ingress.tls }}
  tls:
    {{- range .Values.emsui.ingress.tls }}
    - hosts:
        - {{ $subdomain }}.{{ $endpoint }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.emsui.ingress.hosts }}
    - host: {{ $subdomain }}.{{ $endpoint }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: emsui
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: emsui
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}